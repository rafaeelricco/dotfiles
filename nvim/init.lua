-- [[ Neovim Entry Point ]]
-- This file is the primary entry point for the Neovim configuration.
-- It delegates the main setup to the `lua/init.lua` file, which handles
-- the bootstrapping of all other modules.

require("init")